"use client"; import {useState} from "react"; import Link from "next/link"; export default function Project({params}:{params:{projectId:string}}){const [fixed,setFixed]=useState(false); const unsafe=params.projectId==='unsafe-store'; return <main className="shell legal-page"><div className="prototype-banner">Prototype simulation — not connected to real customer systems.</div><div className="eyebrow">PROJECT OVERVIEW</div><h1>{unsafe?'Unsafe Store Demo':'Safe Notes Demo'}</h1><p>Next.js · Supabase · Latest demo scan just now</p><div className="score-big"><b>{unsafe?(fixed?'100':'72'):'100'}</b><span>/100 safety score</span></div><h2>Findings</h2>{unsafe&&!fixed?<div className="finding-card"><b>CRITICAL</b><h3>Exposed Supabase service-role key</h3><p>src/lib/supabase-client.ts</p><p>A private database key may be included in browser code.</p><button className="button" onClick={()=>setFixed(true)}>Simulate Fix</button></div>:<div className="empty">{fixed?'Finding verified as fixed after demo rescan.':'No findings in this demo.'}</div>}<div className="links"><Link href="/app/projects">← All projects</Link><Link href="/internal/placeholders">View placeholder registry</Link></div></main>}
